{
  "_comment": "Scheduled-run policy (spec 011). apify_scheduled_count is a per-search ceiling, not a typical haul: the LinkedIn search URLs carry f_TPR=r86400 (past 24h), so the actor only returns truly-new jobs. 50 is what the old production wrapper ran; the CLI default for manual runs stays 150.",
  "apify_scheduled_count": 50
}
