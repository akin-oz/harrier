# One LinkedIn public jobs search URL per line.
# geoId=91000007 = Europe, f_TPR=r86400 = posted in last 24h.
# f_WT=2 was LinkedIn's remote filter; its AI search retired it and the Apify
# actor converts it to search keywords, so it only biases results toward
# remote (fewer paid results discarded). Remote-only enforcement happens in
# screening: the posting's workplace declaration is normalized into its
# location, and remote evidence is required in its own text (spec 053).
# f_TPR=r86400 is the key cost-saver: keeps Apify from re-scraping the same
# stale top-N relevance results every run. Daily cron + 24h filter = no gaps.
# Stack-specific (Europe, remote)
https://www.linkedin.com/jobs/search/?geoId=91000007&keywords=senior%20frontend%20engineer&f_WT=2&f_TPR=r86400
