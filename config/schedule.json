{
  "_comment": "The cadence in one place (spec 020, ADR-006). Each job renders one launchd plist that invokes the harrier CLI directly: no shell wrapper, so .env is read by harrier's Python loader. Change a time here, then run `harrier schedule install`. Contains no personal data and no paths: the repo path, user, and interpreter are resolved at install time.",
  "label_prefix": "dev.harrier",
  "jobs": [
    {
      "name": "discovery",
      "command": ["discover", "--scheduled"],
      "trigger": {
        "kind": "calendar",
        "times": [
          { "hour": 9, "minute": 0 },
          { "hour": 13, "minute": 0 },
          { "hour": 16, "minute": 0 },
          { "hour": 20, "minute": 0 }
        ]
      }
    },
    {
      "name": "digest",
      "command": ["digest"],
      "trigger": {
        "kind": "calendar",
        "times": [{ "hour": 20, "minute": 30 }]
      }
    },
    {
      "name": "gmail-watch",
      "command": ["gmail-watch"],
      "trigger": { "kind": "interval", "seconds": 300 }
    }
  ]
}
